// Client-side configuration for API base
export const Config = Object.freeze({
  // Set this to your backend API origin (no trailing slash)
  // Example: https://api.yourdomain.com/api
  apiBaseUrl: 'http://localhost:3000/api'
});
// moved to client/
